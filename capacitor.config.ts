import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.first.app',
  appName: 'first-mobile-project',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
